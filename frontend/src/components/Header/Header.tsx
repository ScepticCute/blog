import React from 'react';
import styles from './Header.module.sass';

export const Header = () => {
  return (
    <div className={styles.wrapper}>
      <nav>
        <ul>
          <li>1</li>
        </ul>
      </nav>
    </div>
  );
};
